# =============================================================================
# DECISION LOGGER BOT - ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to git - it contains sensitive credentials
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
PORT=3000

# -----------------------------------------------------------------------------
# MONGODB DATABASE
# -----------------------------------------------------------------------------
# Get connection string from MongoDB Atlas dashboard
# Format: mongodb+srv://USERNAME:PASSWORD@CLUSTER.mongodb.net/?appName=Decision-logger
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@your-cluster.mongodb.net/?appName=Decision-logger

# -----------------------------------------------------------------------------
# SLACK CONFIGURATION
# -----------------------------------------------------------------------------
# Choose ONE of two modes:
#
# OPTION A: OAuth Multi-Workspace Mode (RECOMMENDED for production)
#   - Supports multiple Slack workspaces
#   - Users install via OAuth
#   - Get these from: https://api.slack.com/apps > Your App > OAuth & Permissions
#
# OPTION B: Single-Workspace Mode (Simple setup for testing)
#   - Only works for one workspace
#   - Requires manual token
#   - Get token from: https://api.slack.com/apps > Your App > OAuth & Permissions

# Required for ALL modes:
SLACK_SIGNING_SECRET=your_signing_secret_here

# For OPTION A (OAuth Multi-Workspace):
SLACK_CLIENT_ID=your_client_id_here
SLACK_CLIENT_SECRET=your_client_secret_here
SLACK_STATE_SECRET=your_cryptographically_secure_random_string_here

# Session secret for dashboard authentication (generate with same command as STATE_SECRET)
SESSION_SECRET=your_cryptographically_secure_random_string_here

# For OPTION B (Single-Workspace):
# SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# -----------------------------------------------------------------------------
# JIRA INTEGRATION (OPTIONAL)
# -----------------------------------------------------------------------------
# If you want to link decisions to Jira epics/tickets
# Get API token from: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_URL=https://your-company.atlassian.net
JIRA_EMAIL=your.email@company.com
JIRA_API_TOKEN=your_jira_api_token_here

# -----------------------------------------------------------------------------
# CLAUDE AI (OPTIONAL)
# -----------------------------------------------------------------------------
# For AI-powered decision extraction from meeting transcripts
# Get API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-your-api-key-here

# Claude model to use (optional - defaults to claude-sonnet-4-5-20250929)
# CLAUDE_MODEL=claude-sonnet-4-5-20250929

# Maximum tokens for Claude responses (optional - defaults to 4096)
# CLAUDE_MAX_TOKENS=4096

# =============================================================================
# SECURITY NOTES:
# =============================================================================
# 1. Generate SLACK_STATE_SECRET using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 2. Keep MongoDB credentials secure - rotate every 90 days
# 3. Jira API tokens have full account access - protect carefully
# 4. Claude API key usage incurs costs - monitor usage
# 5. Never share .env file or commit it to version control
# =============================================================================
